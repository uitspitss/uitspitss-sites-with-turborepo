{
  "name": "uitspitss-sites-turbo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "storybook": "turbo run storybook",
    "format": "prettier --check --write \"**/*.{ts,tsx,md,mdx}\"",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "husky": "^8.0.2",
    "prettier": "latest",
    "turbo": "^1.5.5"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {},
  "resolutions": {
    "glob-parent": "^6.0.0",
    "trim": "^1.0.0",
    "trim-newlines": "^4.0.0",
    "loader-utils": "^2.0.4"
  },
  "packageManager": "yarn@1.22.19"
}
