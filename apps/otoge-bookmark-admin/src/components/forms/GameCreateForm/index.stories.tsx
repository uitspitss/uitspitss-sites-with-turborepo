import { ComponentMeta } from '@storybook/react';

import { GameCreateForm } from '.';

export default {
  component: GameCreateForm,
  args: {},
} as ComponentMeta<typeof GameCreateForm>;

export const Default = {};
