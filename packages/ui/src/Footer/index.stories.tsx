import { ComponentMeta } from '@storybook/react';

import { Footer } from '.';

export default {
  component: Footer,
  title: 'Footer',
} as ComponentMeta<typeof Footer>;

export const Default = {
  args: {},
};
