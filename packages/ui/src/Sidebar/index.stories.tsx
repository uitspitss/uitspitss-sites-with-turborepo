import { ComponentMeta } from '@storybook/react';

import { Sidebar } from '.';

export default {
  component: Sidebar,
  title: 'Sidebar',
} as ComponentMeta<typeof Sidebar>;

export const Default = {
  args: {},
};
